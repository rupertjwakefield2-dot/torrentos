# TorrentOS — NVIDIA kernel module options
# Enable DRM KMS (required for Wayland / Hyprland)
options nvidia_drm modeset=1 fbdev=1

# Preserve VRAM across suspend/resume
options nvidia NVreg_PreserveVideoMemoryAllocations=1
