# TorrentOS

An Arch-based Linux distribution that aims for macOS aesthetics, Windows-grade
accessibility, and Linux developer power.

## Repo Layout

```text
torrentos/
archiso/                     archiso profile consumed by mkarchiso
  profiledef.sh              ISO metadata and build flags
  packages.x86_64            packages installed into the live system
  pacman.conf                pacman config used inside the chroot
  airootfs/                  files copied verbatim into the live root
    etc/skel/                default user dotfiles
    usr/share/torrentos/     branding and assets
packages/                    custom PKGBUILDs hosted in the torrentos repo
  torrentos-base/            meta-package, branding, settings daemon
  torrentos-theme/           GTK/Qt/icon/cursor theme bundle
  torrentos-hyprland-config/ default Hyprland and Waybar config
  torrentos-first-boot/      welcome wizard
  torrentos-settings/        settings application
repo/                        local pacman repo generated by build scripts
scripts/                     build helpers
  build-pkgs.sh              builds custom PKGBUILDs into ./repo/
  build-aur.sh               builds required AUR packages into ./repo/
  build-iso.sh               wraps mkarchiso
  test-iso.sh                boots the newest ISO in QEMU
docs/
  BUILDING.md                build host, workflow, and release notes
```

## Quick Start

You need an **Arch Linux host**: WSL2 with Arch Linux, a VM, or bare metal.
Building from Windows directly is not supported because `mkarchiso` requires a
Linux kernel and loop devices.

```bash
sudo pacman -S --needed archiso base-devel git
cd torrentos
./scripts/build-pkgs.sh
./scripts/build-iso.sh
```

The ISO is written to `./out/torrentos-<date>-x86_64.iso`.

See [docs/BUILDING.md](docs/BUILDING.md) for full instructions.

## Status

v0.3 scaffold. Boots to a Hyprland session with TorrentOS branding, a first-boot
wizard, settings package, local package repo, and ISO build helpers. Installer
and hardware-polish work are still in progress.

## License

GPL-3.0 for distro-specific code. Upstream packages keep their original
licenses.
